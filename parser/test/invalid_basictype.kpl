PROGRAM BASICTYPE; (* INVALID BASICTYPE EXAMPLE *)

VAR N: INTEGER;

FUNCTION F(N: INTEGER) : STRING;
	BEGIN
		IF N = 0 THEN F := 1 ELSE F := N * F(N-1);
	END;

BEGIN
	FOR N := 1 TO 7 DO
		BEGIN
			CALL WRITELN;
			CALL WRITEI(F(N));
		END;
END. (* BASICTYPE ERROR *)